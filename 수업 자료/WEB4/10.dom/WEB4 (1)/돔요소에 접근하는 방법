CSS에서 class 와 id등, 선택자를 지정한 다음 이 선택자들을 기준으로 접근할 수 있다.

    getElementById()
        객체의 아이디를 이용하여 엘리먼트에 접근하는 방식
        아이디는 하나만 사용할수 있으므로 하나만 선택할 수 있다.

    getElementsByClassName()
        객체의 클래스를 이용하여 엘리먼트에 접근하는 방식
        클래스는 여러개 있을수 있어 엘리먼츠가 된다.
        또한, 여러개가 선택될 수 있기때문에 단순한 객체가 아닌 HTML collection 객체로 저장된다.

    getElementsByTagName()
        객체에 클래스나 아이디가 지정되지 않았을경우 이용할 수 있다.
        태그 이름을 통해 엘리먼츠에 접근할 수 있다.
        태그 역시 중복으로 사용할 수 있기 때문에 메서드 이름이 엘리먼츠로 쓰이고
        HTML collection 객체로 저장된다.

    querySelector()
    
    querySelectorAll()

    getAttribute()
    setAttribute()
    